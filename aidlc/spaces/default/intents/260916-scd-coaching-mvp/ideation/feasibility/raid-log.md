# RAID Log — SCD 소통 코칭

Risks(위험) / Assumptions(가정) / Issues(현안) / Dependencies(의존).
출처 태그는 `feasibility-assessment.md` § Sources 와 같다.

위험 점수 = 발생 가능성(높음/중간/낮음) × 영향(높음/중간/낮음).

---

## R — Risks (위험)

| ID | 위험 | 가능성 | 영향 | 대응 전략 | 대응 방안 | 출처 |
|---|---|---|---|---|---|---|
| R1 | 팀의 학습 배경은 React+Java 인데 백엔드를 Python 으로 확정했다. Python 경험이 일부뿐이라 7주 안에 학습과 구현이 겹친다 | 높음 | 높음 | 완화 | 첫 작업 단위를 얇은 종단 슬라이스로 잡아 스택 전체를 일찍 관통시킨다. 전원 풀스택 배정이라 학습이 분산되므로, 초반에 백엔드 코드 규약(계층 경계, 응답 모델, 테스트 형태)을 먼저 고정해 편차를 줄인다 | `[spec:07] D-01, [F1][F5][F6]` |
| R2 | 스택 전환에 따라 기존 명세 9개 항목(§2.1)과 시나리오 원형 6개·UI 문구를 구현 전에 다시 써야 한다 | 높음 | 중간 | 완화 | **부분 해소(2026-09-16)**: `02_tech-environment.md` 재작성 완료(D-50). 남은 것은 시나리오 원형 6개(A-12)와 UI 문구(A-11)다 | `[F1][intent]` |
| R3 | 단일 백엔드로 바뀌면서 원래 "analysis 의 DB 직접 접근 금지"가 지키던 데이터 소유 경계가 사라진다. F07 삭제 전파가 새어 나갈 수 있다 | 중간 | 높음 | 완화 | TC-14 로 계층 경계 규칙을 다시 정의하도록 제약에 올렸다. domain-design(2.6)에서 삭제 전파 범위를 명시적으로 모델링한다 | `[spec:02][F1]` |
| R4 | live 어댑터를 만들기로 했으나 제공자가 미정이라 비용·응답시간·실패 처리를 설계할 수 없다 | 중간 | 중간 | 수용 | live 는 선택 경로이므로 SM1 검증을 막지 않는다. 제공자가 정해질 때까지 인터페이스만 고정하고 live 구현체 작성을 미룬다 | `[Q7][F2][F3]` |
| R5 | 마감 2026-11-05 까지 약 7주인데, F01~F07 전체와 스택 학습·명세 재작성이 같은 기간에 들어간다 | 중간 | 높음 | 완화 | delivery-planning(2.9)에서 F01~F07 을 작업 단위로 쪼갠 뒤 이 평가를 재검증한다. 범위를 줄여야 한다면 SC-03 의 F 항목 단위로 사용자와 논의한다 | `[F4][F6]` |
| R6 | Mock 이 기대 판정을 고정 반환하므로 SM1 은 통과하지만, 규칙 엔진의 실제 정확성은 검증되지 않는다 | 중간 | 중간 | 수용 | 의도된 설계다. 이번 목적은 흐름 성립 확인이다. 집계 규칙(보류·기회없음 분모 제외, 독립/도움 후 분리)은 별도 단위 테스트로 직접 검증한다. **갱신(2026-09-17)**: 그 단위 테스트의 조건이 확정되었다 — 테스트는 전부 구현 후에 쓰고(test-after), 커버리지 바닥은 `service/` 라인 80% 하나뿐이라 집계 함수의 여러 갈래를 강제하지 않으며, 종단 실행도 기대 판정과 대조하지 않는다. 대신 `[spec:05]` 의 기대 판정을 픽스처 파일로 고정하고 단위 테스트가 그것을 읽는 의무가 새로 들어왔다. 즉 기대값의 출처는 고정되었고, 여러 갈래를 실제로 보는지는 테스트 작성자의 판단에 남는다 | `[intent][spec:02][F7]` |
| R7 | EXECUTE 단계가 두 번 늘어 **19 → 20 → 21** 이 되었다. 첫 증가는 approval-handoff 의 user-stories(2.4) 추가, 두 번째는 practices-discovery 후속으로 ci-pipeline(3.7)을 다시 넣은 것이다. 마감 2026-11-05 과 R5 는 그대로인 채 부담이 두 번 늘었다 | 중간 | 중간 | 수용 | 두 추가 모두 어느 단계도 맡지 않은 공백을 메우는 것이다 — 2.4 는 수용 기준을 쓸 단계가 없던 공백, 3.7 은 GitHub Actions 워크플로를 만들 단계가 없던 공백이다. 종합 판정은 진행(Go)으로 유지하되 delivery-planning(2.9)의 일정 재검증에 **두 증가분을 모두** 포함한다 | approval-handoff Q3, Q4; `[F7]` |

## A — Assumptions (가정)

| ID | 가정 | 확인 방법 | 틀렸을 때 | 출처 |
|---|---|---|---|---|
| A1 | `localhost` 가 보안 컨텍스트로 취급되어 HTTPS 없이 MediaRecorder 녹음이 동작한다 | 첫 작업 단위에서 브라우저 녹음을 실제로 한 번 돌려 본다 | 로컬 개발용 HTTPS 인증서 설정이 추가로 필요해진다 | `[spec:02]` |
| A2 | 이번 범위에서 규제 항목을 기록하지 않아도 되는 이유는 데이터가 전부 합성이기 때문이다. 실사용 데이터로 확장하면 이 가정은 즉시 무효가 된다 | SC-02(합성 데이터 전용)가 유지되는지 단계마다 확인한다 | 개인정보보호법·아동청소년 관련 요건을 뒤늦게 설계에 끼워 넣어야 한다 | `[Q5][intent]` |
| A3 | live 어댑터는 선택 경로이므로 이번 검증의 성패에 영향을 주지 않는다 | SM1 을 Mock 경로로 통과시켜 확인한다 | live 가 기본 경로로 올라가면 SM1 의 검증 기준을 다시 정의해야 한다 | `[F2]` |
| A4 | 집계 규칙 로직(`[spec:02]` 7.2 의 Java 예시)은 언어 중립적이라 Python 이식 위험이 낮다 | 이식한 코드에 원래 테스트 케이스(보류·기회없음 분모 제외)를 그대로 옮겨 통과시킨다 | 이식 과정에서 판정 규칙이 미묘하게 달라져 기대 판정과 어긋난다 | `[spec:02]` |
| A5 | 5인 전원 풀스택 배정이 학습 부담을 분산시킨다 | 첫 작업 단위 종료 시점에 진행 속도를 본다 | 전원이 동시에 학습 곡선을 타면서 오히려 초반 속도가 더 느려진다 | `[F5][F6]` |
| A6 | 원격 저장소(GitHub 등)를 만들 수 있고 GitHub Actions 를 쓸 수 있다. 병합 전 검사를 Actions 로 강제한다는 결정(TC-16)과 ci-pipeline(3.7)의 존재 이유가 전부 이 전제 위에 서 있다 | 저장소를 실제로 만드는 시점에 원격 생성과 Actions 실행을 한 번 확인한다 | 병합 게이트를 `pre-push` 훅 같은 로컬 수단으로 다시 짜야 하고, ci-pipeline(3.7)의 산출물과 작업 분기 흐름도 함께 바뀐다 | `[F7]` |

## I — Issues (현안 — 지금 처리해야 하는 것)

| ID | 현안 | 영향 | 처리 방안 | 담당 단계 | 출처 |
|---|---|---|---|---|---|
| I1 | ~~결정 D-01 의 상태가 "확정"인 채로 남아 있는데 내용이 뒤집혔다~~ | — | **해소됨(2026-09-16)**: D-01 상태를 "변경됨 → D-30"으로 바꾸고 D-30 을 추가했다 | 완료 | `[spec:07][F1]` |
| I2 | ~~승인된 의도 서술의 "실제 STT·LLM 연동은 이 목적에 필요하지 않다"가 F2 결정과 어긋난다~~ | — | **해소됨(2026-09-16)**: approval-handoff 에서 의도 서술을 갱신했다(D-49) | 완료 | `[intent][F2]` |
| I3 | ~~`[spec:02]` 문서 전체가 Java 전제로 쓰여 있어 지금 그대로는 하위 단계 입력으로 쓸 수 없다~~ | — | **해소됨(2026-09-16)**: approval-handoff 에서 Python·FastAPI 전제로 재작성했다(D-50) | 완료 | `[spec:02][F1]` |
| I4 | 요약 브리프 생성 도구(`review-brief summary`)가 오류로 동작하지 않았다 | 확인 절차 자체는 규정대로 수행했으나 자동 브리프가 없다 | 프레임워크 쪽 문제이며 이 프로젝트 산출물에는 영향이 없다. 재발 시 `/aidlc --doctor` 로 확인한다 | — | 이 단계 실행 기록 |

## D — Dependencies (의존)

| ID | 의존 | 종류 | 차단 여부 | 필요 시점 | 출처 |
|---|---|---|---|---|---|
| D1 | 시나리오 원형 6개와 UI 문구의 중·고등학교 배경 재작성 | 내부 산출물 | F05 모의 대화 구현을 차단한다 | code-generation 이전 | `[intent]` |
| D2 | live 어댑터의 STT·LLM 제공자 선택 | 외부 결정 | live 구현체 작성만 차단한다. Mock 경로는 차단하지 않는다 | live 구현 착수 시점 | `[F3]` |
| D3 | ~~ORM·마이그레이션 도구·패키지 관리자 선택~~ | 내부 결정 | **해소됨(2026-09-17)**: practices-discovery(2.2)에서 SQLAlchemy 2.0(동기) / Alembic / uv 로 확정되었다(TC-15). 더 이상 차단하지 않는다 | 완료 | `[F1][F7]` |
| D4 | 합성 테스트 대화 3건과 기대 판정 (`[spec:05]`) | 기존 자산 | 이미 존재하며 그대로 쓴다. 차단하지 않는다 | SM1 검증 시점 | `[intent][spec:05]` |
| D5 | Docker 런타임 (frontend, backend, postgres 3개 컨테이너) | 환경 | 제약 없음이 확인되었다. 차단하지 않는다 | 로컬 실행 시점 | `[Q3]` |

## Assumptions & Open Questions

None.
