<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations
- 2026-09-17T01:17:32Z — 되돌아온 회차의 질문을 만들 때, "무엇을 고칠까요"를 열린 채로 묻지 않고 이 단계가 끝난 뒤 하위 단계가 실제로 바꾼 사실을 먼저 대조해 네 항목으로 좁혔다. 미확정으로 열어 둔 항목(TC-15, D3)이 닫혔는지, 새 전제가 들어왔는지, 수치를 적은 항목의 수치가 변했는지, 대응 방안이 기대던 수단의 조건이 달라졌는지 — 이 네 가지가 대조의 축이 되었다.
- 2026-09-17T01:17:32Z — GitHub Actions 를 제약으로 올리면서 SC-01(클라우드 배포 없음)과 충돌하지 않는다는 문장을 함께 넣었다. 두 항목이 나란히 있으면 하위 단계가 모순으로 읽을 자리가 생기는데, 구분선을 같은 문서 안에 두는 편이 나중에 설명하는 것보다 싸다.

- 2026-09-16T05:30:00Z — 질문을 7개로 구성했다; Standard 깊이(5-8)의 상한 근처를 택했다. intent-capture가 제품 경계·대상·성공 기준을 이미 닫아 두어 그 영역은 다시 묻지 않고, 기존 명세에 "확인 필요"로 남아 있던 가정(A-05~A-09)과 D-01 스택 분기에만 질문을 집중했다.
- 2026-09-16T05:30:00Z — market-research(1.2)가 SKIP이라 선택 입력 세 건의 부재를 설계상 예상된 것으로 처리했다; 누락이 아니라 범위 밖임을 질문 파일과 타당성 평가서의 Sources 절에 명시해, 하위 단계가 이를 결손으로 오해하지 않게 했다.
- 2026-09-16T05:45:00Z — 확정 항목을 뒤집는 답변 2건을 바로 반영하지 않고 영향 항목을 열거한 뒤 확인받았다; Q1=B(단일 백엔드)와 Q2=A(Java 포함 조합 확정)가 직접 충돌했고 Q7=C(live 실제 구현)는 승인된 의도 서술과 충돌했다. project.md의 학습 규칙에 따라 각 변경이 무효화하는 확정 항목을 표로 만들어 F1·F2에서 함께 확인했다.
- 2026-09-16T05:45:00Z — 선택지가 요구한 부가 정보가 비어 있을 때 임의로 채우지 않고 분리 질문으로 돌렸다; Q4(마감 일자)와 Q7(제공자)이 그랬고 F3·F4로 나눴다. 추정으로 채웠다면 이후 단계가 근거 없는 값을 물려받았을 것이다.
- 2026-09-16T05:45:00Z — Q5=C(규제 항목 기록 안 함)를 F07 정정·삭제 및 보호자 권한과의 모순으로 보지 않았다; 선택지 문구가 "규제 항목"을 가리키고 그 기능들은 intent-capture에서 제품 기능으로 확정되었기 때문이다. 이 해석은 통합 요약 확인 단계에서 사용자에게 다시 확인받았다.
- 2026-09-16T05:55:00Z — F4 답변에서 "개발 인원 5인"이 처음 드러나 이 단계에서 인원·역할을 확정했다; Q4 선택지 D(외부 마감)에는 인원 정보가 없었고 team-formation(1.5)이 SKIP이라 다른 어느 단계도 묻지 않는 값이었다. F5·F6으로 역량과 역할 분담을 함께 닫았다.

## Deviations

- 2026-09-16T05:55:00Z — 통합 요약 확인 직전에 실행하도록 되어 있는 요약 브리프 생성을 건너뛰고 진행했다; `review-brief summary`가 도구 내부 오류로 실패했다. 확인 체크포인트 자체(결정 기록 → 질문 → 답변 영수증)는 규정대로 수행했고 브리프는 표시 보조 수단이라 재시도 없이 요약을 직접 작성해 제시했다.

## Tradeoffs
- 2026-09-17T01:17:32Z — 위험 R6 의 대응 방안을 고쳐 쓰면서, 확정된 선택이 남기는 빈자리(집계 함수의 여러 갈래를 실제로 보는지는 바닥이 강제하지 않는다)를 지우지 않고 그대로 적었다. 사람이 알고 고른 절충이므로 결함으로 쓰지 않되, 보이지 않게 두지도 않는 쪽을 택했다.

- 2026-09-16T06:05:00Z — 제약 대장의 "규제 제약" 절을 비우되 비운 이유와 재개 조건을 본문에 적었다; 빈 절을 그대로 두면 "규제가 없다"로 읽힐 수 있어, 사용자 결정에 따른 의도적 공백이며 실사용 데이터로 확장하면 다시 채워야 한다는 문장을 넣는 쪽을 택했다.
- 2026-09-16T06:05:00Z — 스택 전환이 무효화한 확정 항목 9건을 타당성 평가서 §2.1 표로 남겼다; 판정 근거와 후속 명세 재작성 작업 목록을 한 표로 겸하게 한 선택이다. 둘을 나누면 같은 내용이 두 문서에 중복되고 한쪽만 갱신될 위험이 있어 RAID의 R2·I3이 이 표를 가리키게 했다.
- 2026-09-16T06:05:00Z — 단일 백엔드 전환이 F07 삭제 전파에 유리하다고 판단했다; 원래 구조는 "analysis는 DB에 접근하지 않는다"는 규칙으로 삭제 누락을 막았는데 분리 자체가 사라지면 그 위험원도 사라지기 때문이다. 다만 계층 경계 규칙을 새로 정의하지 않으면 같은 누락이 다른 형태로 생길 수 있어 TC-14와 R3으로 남겼다.

## Open questions

- 2026-09-16T06:05:00Z — live 어댑터의 STT·LLM 제공자가 미정이라 비용·응답시간·실패 처리를 이 단계에서 평가할 수 없었다; 제공자가 정해지면 PM-02(Mock 기준 3초)와 별개로 live 응답시간 목표를 세워야 한다.
- 2026-09-16T06:05:00Z — 7주·5인이 F01~F07에 충분한지는 추정이며 실측이 아니다; delivery-planning(2.9)에서 작업 단위가 나온 뒤 반드시 재검증해야 한다.
